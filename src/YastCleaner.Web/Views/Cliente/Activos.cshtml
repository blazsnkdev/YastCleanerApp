@model YastCleaner.Web.Utils.PaginaResult<YastCleaner.Web.ViewModels.ClienteViewModel>

@{
    ViewData["Title"] = "Activos";
}

<h1 class="text-center">Clientes Activos</h1>

<p>
    <a asp-action="Registrar" class="btn btn-success">Agregar Nuevo Cliente</a>
</p>

@if (Model.Items.Any())
{
    <div class="d-flex justify-content-between align-items-center mb-3">
        <div class="text-muted">
            Mostrando @((Model.PaginaIndice - 1) * Model.TamanioPagina + 1) - @Math.Min(Model.PaginaIndice * Model.TamanioPagina, Model.TotalRegistros) de @Model.TotalRegistros registros
        </div>
        <div>
            <span class="me-2">Registros por página:</span>
            <div class="btn-group btn-group-sm">
                <a asp-action="Activos" asp-route-pagina="1" asp-route-tamanioPagina="5" class="btn btn-outline-primary @(Model.TamanioPagina == 5 ? "active" : "")">5</a>
                <a asp-action="Activos" asp-route-pagina="1" asp-route-tamanioPagina="10" class="btn btn-outline-primary @(Model.TamanioPagina == 10 ? "active" : "")">10</a>
                <a asp-action="Activos" asp-route-pagina="1" asp-route-tamanioPagina="25" class="btn btn-outline-primary @(Model.TamanioPagina == 25 ? "active" : "")">25</a>
                <a asp-action="Activos" asp-route-pagina="1" asp-route-tamanioPagina="50" class="btn btn-outline-primary @(Model.TamanioPagina == 50 ? "active" : "")">50</a>
            </div>
        </div>
    </div>

    <table class="table table-striped table-hover">
        <thead class="table-dark">
            <tr>
                <th>Nombre</th>
                <th>Apellido Paterno</th>
                <th>Apellido Materno</th>
                <th>Número Celular</th>
                <th>Dirección</th>
                <th>Email</th>
                <th>Estado</th>
                <th>Fecha de Registro</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Items)
            {
                <tr>
                    <td>@Html.DisplayFor(modelItem => item.Nombre)</td>
                    <td>@Html.DisplayFor(modelItem => item.ApellidoPaterno)</td>
                    <td>@Html.DisplayFor(modelItem => item.ApellidoMaterno)</td>
                    <td>@Html.DisplayFor(modelItem => item.NumeroCelular)</td>
                    <td>@Html.DisplayFor(modelItem => item.Direccion)</td>
                    <td>@Html.DisplayFor(modelItem => item.Email)</td>
                    <td>
                        <span class="badge bg-success">@Html.DisplayFor(modelItem => item.Estado)</span>
                    </td>
                    <td>@Html.DisplayFor(modelItem => item.FechaRegistro)</td>
                    <td>
                        <a asp-controller="Cliente" asp-action="Detalle" asp-route-clienteId="@item.ClienteId" class="btn btn-info btn-sm">Detalle</a>
                        <br />
                        <a asp-controller="Cliente" asp-action="Actualizar" asp-route-clienteId="@item.ClienteId" class="btn btn-warning btn-sm">Actualizar</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>

    <!-- Controles de paginación -->
    <nav aria-label="Paginación de clientes">
        <ul class="pagination justify-content-center">
            <li class="page-item @(!Model.TienePaginaAnterior ? "disabled" : "")">
                <a class="page-link" asp-action="Activos" asp-route-pagina="1" asp-route-tamanioPagina="@Model.TamanioPagina" aria-label="Primera">
                    <span aria-hidden="true">&laquo;&laquo;</span>
                </a>
            </li>
            <li class="page-item @(!Model.TienePaginaAnterior ? "disabled" : "")">
                <a class="page-link" asp-action="Activos" asp-route-pagina="@(Model.PaginaIndice - 1)" asp-route-tamanioPagina="@Model.TamanioPagina" aria-label="Anterior">
                    <span aria-hidden="true">&laquo;</span>
                </a>
            </li>

            @{
                int inicio = Math.Max(1, Model.PaginaIndice - 2);
                int fin = Math.Min(Model.TotalPagias, Model.PaginaIndice + 2);
            }

            @for (int i = inicio; i <= fin; i++)
            {
                <li class="page-item @(i == Model.PaginaIndice ? "active" : "")">
                    <a class="page-link" asp-action="Activos" asp-route-pagina="@i" asp-route-tamanioPagina="@Model.TamanioPagina">@i</a>
                </li>
            }

            <li class="page-item @(!Model.TienePaginaSiguiente ? "disabled" : "")">
                <a class="page-link" asp-action="Activos" asp-route-pagina="@(Model.PaginaIndice + 1)" asp-route-tamanioPagina="@Model.TamanioPagina" aria-label="Siguiente">
                    <span aria-hidden="true">&raquo;</span>
                </a>
            </li>
            <li class="page-item @(!Model.TienePaginaSiguiente ? "disabled" : "")">
                <a class="page-link" asp-action="Activos" asp-route-pagina="@Model.TotalPagias" asp-route-tamanioPagina="@Model.TamanioPagina" aria-label="Última">
                    <span aria-hidden="true">&raquo;&raquo;</span>
                </a>
            </li>
        </ul>
    </nav>
}
else
{
    <div class="alert alert-info mt-4" role="alert">
        No hay clientes activos en este momento.
    </div>
}