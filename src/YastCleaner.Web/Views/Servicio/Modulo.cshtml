@model YastCleaner.Web.Utils.PaginaResult<YastCleaner.Web.ViewModels.ServicioViewModel>

@{
    ViewData["Title"] = "Modulo";
}

<h1>Módulo de Servicios</h1>

<p>
    <a asp-action="RegistrarServicio" class="btn btn-success">Crear Nuevo Servicio</a>
    <a asp-controller="Servicio" asp-action="Modulo" asp-route-estado="Inactivo" class="btn btn-warning">Ver servicios inactivos
</a>

</p>

@if (Model.Items.Any())
{
    <div class="d-flex justify-content-between align-items-center mb-3">
        <div class="text-muted">
            Mostrando @((Model.PaginaIndice - 1) * Model.TamanioPagina + 1) - @Math.Min(Model.PaginaIndice * Model.TamanioPagina, Model.TotalRegistros) de @Model.TotalRegistros servicios
        </div>
        <div>
            <span class="me-2">Registros por página:</span>
            <div class="btn-group btn-group-sm">
                <a asp-action="Modulo" asp-route-pagina="1" asp-route-tamanioPagina="5" class="btn btn-outline-primary @(Model.TamanioPagina == 5 ? "active" : "")">5</a>
                <a asp-action="Modulo" asp-route-pagina="1" asp-route-tamanioPagina="10" class="btn btn-outline-primary @(Model.TamanioPagina == 10 ? "active" : "")">10</a>
                <a asp-action="Modulo" asp-route-pagina="1" asp-route-tamanioPagina="15" class="btn btn-outline-primary @(Model.TamanioPagina == 15 ? "active" : "")">15</a>
                <a asp-action="Modulo" asp-route-pagina="1" asp-route-tamanioPagina="20" class="btn btn-outline-primary @(Model.TamanioPagina == 20 ? "active" : "")">20</a>
            </div>
        </div>
    </div>

    <table class="table table-striped table-hover">
        <thead class="table-dark">
            <tr>
                <th>Nombre</th>
                <th>Precio</th>
                <th>Descripción</th>
                <th>Estado</th>
                <th>Fecha de Registro</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Items)
            {
                <tr>
                    <td>@Html.DisplayFor(modelItem => item.Nombre)</td>
                    <td>@Html.DisplayFor(modelItem => item.Precio)</td>
                    <td>@Html.DisplayFor(modelItem => item.Descripcion)</td>
                    <td>
                        <span class="badge @(item.Estado == "Disponible" ? "bg-success" : "bg-secondary")">
                            @Html.DisplayFor(modelItem => item.Estado)
                        </span>
                    </td>
                    <td>@Html.DisplayFor(modelItem => item.FechaRegistro)</td>
                    <td>
                        <form asp-action="ManipularEstado" method="post">
                            <input type="hidden" name="servicioId" value="@item.ServicioId" />

                            @if (item.Estado == "Disponible")
                            {
                                <button type="submit" class="btn btn-warning btn-sm"
                                onclick="return confirm('¿Seguro que desea Desactivar ese servicio?')"
                                >Desactivar</button>
                            }
                            else
                            {
                                <button type="submit" class="btn btn-success btn-sm"
                                onclick="return confirm('¿Seguro que desea Activar ese servicio?')"
                                >Activar</button>
                            }
                        </form>

                        <a asp-action="Detalle" asp-route-servicioId="@item.ServicioId" >Ver Detalle</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>

    <!-- Controles de paginación -->
    <nav aria-label="Paginación de servicios">
        <ul class="pagination justify-content-center">
            <li class="page-item @(!Model.TienePaginaAnterior ? "disabled" : "")">
                <a class="page-link" asp-action="Modulo" asp-route-pagina="1" asp-route-tamanioPagina="@Model.TamanioPagina" aria-label="Primera">
                    <span aria-hidden="true">&laquo;&laquo;</span>
                </a>
            </li>
            <li class="page-item @(!Model.TienePaginaAnterior ? "disabled" : "")">
                <a class="page-link" asp-action="Modulo" asp-route-pagina="@(Model.PaginaIndice - 1)" asp-route-tamanioPagina="@Model.TamanioPagina" aria-label="Anterior">
                    <span aria-hidden="true">&laquo;</span>
                </a>
            </li>

            @{
                int inicio = Math.Max(1, Model.PaginaIndice - 2);
                int fin = Math.Min(Model.TotalPagias, Model.PaginaIndice + 2);
            }

            @for (int i = inicio; i <= fin; i++)
            {
                <li class="page-item @(i == Model.PaginaIndice ? "active" : "")">
                    <a class="page-link" asp-action="Modulo" asp-route-pagina="@i" asp-route-tamanioPagina="@Model.TamanioPagina">@i</a>
                </li>
            }

            <li class="page-item @(!Model.TienePaginaSiguiente ? "disabled" : "")">
                <a class="page-link" asp-action="Modulo" asp-route-pagina="@(Model.PaginaIndice + 1)" asp-route-tamanioPagina="@Model.TamanioPagina" aria-label="Siguiente">
                    <span aria-hidden="true">&raquo;</span>
                </a>
            </li>
            <li class="page-item @(!Model.TienePaginaSiguiente ? "disabled" : "")">
                <a class="page-link" asp-action="Modulo" asp-route-pagina="@Model.TotalPagias" asp-route-tamanioPagina="@Model.TamanioPagina" aria-label="Última">
                    <span aria-hidden="true">&raquo;&raquo;</span>
                </a>
            </li>
        </ul>
    </nav>
}
else
{
    <div class="alert alert-info mt-4" role="alert">
        No hay servicios registrados en este momento.
    </div>
}